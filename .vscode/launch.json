{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Agent Tests",
            "program": "${workspaceFolder}/bazel-bin/agent_test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "sourceMap": {
                "/proc/self/cwd": "${workspaceFolder}"
            },
            "preLaunchTask": "Build Agent Tests"
        }
    ]
}
